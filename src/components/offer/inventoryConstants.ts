/**
 * Shared constants for the inventory modals (staged authoring + live management).
 */

/** Debounce (ms) for the code-search inputs so typing does not re-filter / re-query
 *  on every keystroke. Kept here so both modals use one value, not a magic number. */
export const SEARCH_DEBOUNCE_MS = 350;
